# this script permit to start the display.py script at the boot of the raspberry pi
# you can test it by running the following command: sudo sh ./start.sh

cd /home/pi # move to the project directory
sudo python display.py
